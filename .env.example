# InfiniteTalk 环境变量配置

# 服务端口
PORT=7860

# GPU 空闲超时（秒）
# 模型在 GPU 上空闲超过此时间后，自动转移到 CPU
GPU_IDLE_TIMEOUT=60

# GPU 设备 ID（自动选择，无需手动设置）
# start.sh 会自动选择显存占用最少的 GPU
NVIDIA_VISIBLE_DEVICES=0

# CUDA 设备 ID
CUDA_VISIBLE_DEVICES=0

# 模型存储路径（容器内）
MODEL_PATH=/app/models

# 上传文件临时目录
UPLOAD_DIR=/tmp/infinitetalk_uploads

# 结果输出目录
RESULT_DIR=/tmp/infinitetalk_results
